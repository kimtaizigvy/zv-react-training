import React from 'react';

function LoginHeader () {
    return (
        <header style={{ backgroundColor: '#ffff',height: 45 }}>
            <p style={{marginLeft: 10}}>Router Demo App</p>
        </header>
    );
}

export default LoginHeader